# Dependencies
node_modules/

# Build output (run `cd mcp && npm run build`)
dist/
*.tsbuildinfo

# Secrets / local config
.env

# Scan artifacts — never commit reports or screenshots of audited sites
reports/
*.png
.playwright-mcp/
test-reports/

# OS / editor
.DS_Store
